import 'package:flutter/material.dart';

class SeenScreen extends StatelessWidget {
  const SeenScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Hello there from seen');
  }
}
